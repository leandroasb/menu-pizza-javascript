/**
* @description Simple return Document.QuerySelector
* @param {string} document.querySelector {string}
* @returns {string} Element Select
*/
const QS = document.querySelector.bind(document)

/**
* @description Simple return Document.QuerySelectorAll
* @param {string} document.querySelectorAll {string}
* @returns {string} All Elements Selector
*/
const QSA = document.querySelectorAll.bind(document)
